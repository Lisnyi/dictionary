import { FirstSection, Container } from '../../styles'

export default function NotFound() {
    return (
      <Container>
        <FirstSection>
          Page not found
        </FirstSection>
      </Container>
    )
  }